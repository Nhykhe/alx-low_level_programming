#include "main.h"
#include <stdio.h>

/**
 * mul - a function that multiplies two integers
 * @a: first input
 * @b: second input
 * Return: multiplication of two inputs
 */
int mul(int a, int b)
{
int result = a * b;
return (result);
}
